extends ../layout/base
include ../mixins/video
block content 
    form(method="GET")
        input(placeholder="title of video" name="title" type="text" required)
        input(type="submit" value="search of video")
    each detail in videos 
        +video(detail)
    include ../partials/footer.pug